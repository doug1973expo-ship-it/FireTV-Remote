name: Build Android APK

on:
  push:
    branches: ["main", "master"]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Check out project
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "8.9"

      - name: Build debug APK
        run: gradle --no-daemon assembleRelease

      - name: Publish direct APK download
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release view latest >/dev/null 2>&1 || gh release create latest --title "Fire TV Remote APK" --notes "Latest installable Android APK."
          gh release upload latest app/build/outputs/apk/release/app-release.apk --clobber

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: firetv-remote-install
          path: app/build/outputs/apk/release/app-release.apk
          if-no-files-found: error
